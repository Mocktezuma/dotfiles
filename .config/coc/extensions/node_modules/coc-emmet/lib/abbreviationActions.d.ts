import { Node } from 'EmmetNode';
import { Position, Range, TextDocument } from 'vscode-languageserver-protocol';
/**
 * Checks if given position is a valid location to expand emmet abbreviation.
 * Works only on html and css/less/scss syntax
 * @param document current Text Document
 * @param rootNode parsed document
 * @param currentNode current node in the parsed document
 * @param syntax syntax of the abbreviation
 * @param position position to validate
 * @param abbreviationRange The range of the abbreviation for which given position is being validated
 */
export declare function isValidLocationForEmmetAbbreviation(document: TextDocument, rootNode: Node | undefined, currentNode: Node | null, syntax: string, position: Position, abbreviationRange: Range): boolean;
