"use strict";
/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
Object.defineProperty(exports, "__esModule", { value: true });
const vscode_html_languageservice_1 = require("vscode-html-languageservice");
function parseTagSet(source) {
    const tagSet = {};
    let rawTagSet;
    try {
        rawTagSet = JSON.parse(source);
    }
    catch (err) {
        return {};
    }
    rawTagSet.tags.forEach(c => {
        tagSet[c.label] = new vscode_html_languageservice_1.HTMLTagSpecification(c.description, c.attributes.map(a => a.label));
    });
    return tagSet;
}
exports.parseTagSet = parseTagSet;
function parseAttributes(source) {
    const attributeSet = {};
    let rawAttributeSet;
    try {
        rawAttributeSet = JSON.parse(source);
    }
    catch (err) {
        return {};
    }
    rawAttributeSet.attributes.forEach(ag => {
        attributeSet[ag.label] = Object.assign({}, ag);
    });
    return attributeSet;
}
exports.parseAttributes = parseAttributes;
//# sourceMappingURL=tagDefinitions.js.map